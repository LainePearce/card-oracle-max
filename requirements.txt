# Qdrant
qdrant-client[grpc]>=1.8.0

# OpenSearch (extant cluster — read-only scroll + KNN experiment control)
opensearch-py>=2.4.0
requests-aws4auth>=1.3.0

# Elasticsearch (new self-managed document cluster — write target)
# ES 8.x client API is compatible with the operations we use (_bulk, _index_template, etc.)
elasticsearch[async]>=8.13.0

# MySQL (RDS backfill source — primary and secondary)
pymysql>=1.1.0

# Embedding models
torch>=2.0.0
open-clip-torch>=2.24.0
sentence-transformers>=2.7.0
transformers>=4.38.0

# Data
numpy>=1.26.0
pandas>=2.1.0
pyarrow>=14.0.0

# AWS
boto3>=1.34.0
botocore>=1.34.0

# Experiment / statistics
scipy>=1.12.0
scikit-learn>=1.4.0

# Async
aiohttp>=3.9.0
httpx>=0.26.0

# Web server (GPU worker + search UI)
flask>=3.0.0
gunicorn>=21.2.0
pillow>=10.0.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.0
tqdm>=4.66.0
loguru>=0.7.0
